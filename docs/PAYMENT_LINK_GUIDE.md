# Payment Link System - คู่มือการใช้งาน

## 🎯 Concept ใหม่

**ก่อนหน้า:** เราเลือกวิธีชำระ → แสดง QR/Redirect  
**ตอนนี้:** เราสร้าง Link → ส่งให้ลูกค้า → **ลูกค้าเลือกวิธีชำระเอง**

---

## 📋 Setup Database

รันคำสั่ง SQL นี้ในฐานข้อมูล:

```sql
CREATE TABLE IF NOT EXISTS payment_links (
  id VARCHAR(36) PRIMARY KEY,
  token VARCHAR(64) UNIQUE NOT NULL,
  quotationId VARCHAR(36),
  invoiceId VARCHAR(36),
  amount DECIMAL(10, 2) NOT NULL,
  description TEXT,
  status ENUM('ACTIVE', 'USED', 'EXPIRED', 'CANCELLED') DEFAULT 'ACTIVE',
  expiresAt DATETIME NOT NULL,
  usedAt DATETIME,
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
  updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  INDEX idx_token (token),
  INDEX idx_quotation (quotationId),
  INDEX idx_invoice (invoiceId),
  INDEX idx_status (status),
  
  FOREIGN KEY (quotationId) REFERENCES quotations(id) ON DELETE SET NULL,
  FOREIGN KEY (invoiceId) REFERENCES invoices(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 🔄 Flow การใช้งาน

### 1. สร้าง Payment Link
```
พนักงาน:
1. เข้า Quotation Dashboard → Tab "การชำระเงินลูกค้า"
2. กดปุ่ม "สร้าง Payment Link"
3. ระบบแสดง Modal พร้อม:
   - QR Code (สำหรับสแกนเปิดลิงก์)
   - URL Link (สำหรับคัดลอกส่ง)
   - วันหมดอายุ (default 3 วัน)
4. คัดลอก Link ส่งให้ลูกค้าผ่าน LINE/Email/SMS
```

### 2. ลูกค้าเปิดลิงก์
```
ลูกค้า:
1. กดลิงก์ → เปิดหน้าชำระเงิน (ไม่ต้อง login)
2. เห็นข้อมูล:
   - ชื่อลูกค้า
   - แพ็คเกจทัวร์
   - ยอดชำระ
   - วันหมดอายุ
3. เลือกวิธีชำระเอง:
   ☑️ พร้อมเพย์ (PromptPay) - แสดง QR Code
   ☑️ บัตรเครดิต/เดบิต - Redirect ไปกรอกข้อมูลบัตร
   ☑️ โมบายแบงก์กิ้ง - เลือกธนาคาร → Redirect
4. ชำระเงิน
5. ระบบอัปเดตสถานะอัตโนมัติ
```

### 3. Webhook อัปเดต
```
Payment Gateway:
1. ลูกค้าชำระเงินสำเร็จ
2. Gateway ส่ง Webhook → /api/payment-gateway/webhook
3. ระบบอัปเดต:
   - Transaction status = CONFIRMED
   - สร้างใบเสร็จอัตโนมัติ
   - อัปเดตสถานะใบแจ้งหนี้
   - Mark payment link เป็น USED
```

---

## 📁 ไฟล์ที่สร้าง

| ไฟล์ | หน้าที่ |
|------|---------|
| `app/api/payment-links/route.ts` | API สร้าง/ดึง payment link |
| `app/payment/[token]/page.tsx` | หน้า public สำหรับลูกค้าเปิด |
| `migrations/create_payment_links.sql` | SQL สร้างตาราง |

---

## 🔧 Environment Variables

```env
# Base URL สำหรับสร้าง payment link
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# Payment Gateway (ตามเดิม)
PAYMENT_PUBLIC_KEY=pkey_test_xxxxx
PAYMENT_SECRET_KEY=skey_test_xxxxx
PAYMENT_WEBHOOK_SECRET=whsec_xxxxx
```

---

## ✅ ข้อดีของ Payment Link

1. **ลูกค้าเลือกเอง** - ไม่ต้องบอกวิธีชำระล่วงหน้า
2. **ส่งได้หลายช่องทาง** - LINE, Email, SMS, Facebook
3. **QR Code สะดวก** - สแกนแล้วเปิดลิงก์ได้เลย
4. **ไม่ต้อง Login** - ลูกค้าเข้าได้ทันที
5. **ตั้งวันหมดอายุได้** - ป้องกันลิงก์โดนใช้ผิด
6. **Track ได้** - เช็คว่าลูกค้าเปิดลิงก์หรือยัง

---

## 🧪 ทดสอบ

1. เข้า Quotation Dashboard: `/quotations/{id}/dashboard?tab=customer-payment`
2. กดปุ่ม **"สร้าง Payment Link"**
3. คัดลอก URL หรือสแกน QR Code
4. เปิดในเบราว์เซอร์ใหม่ (หรือโหมด Incognito)
5. ทดสอบเลือกวิธีชำระต่างๆ

---

## 🎨 UI หน้าชำระเงิน

- **Design:** Modern gradient (blue-indigo)
- **Responsive:** รองรับทั้ง mobile และ desktop
- **Payment Methods:** 
  - PromptPay: แสดง QR Code + Countdown
  - Credit Card: Redirect to secure page
  - Banking: เลือกธนาคาร → Redirect

---

## 🔒 Security

- Token แบบ random 64 characters
- ตรวจสอบ expiry date
- สถานะ USED ป้องกันใช้ซ้ำ
- Webhook signature verification (production)

---

## 📊 สถานะ Payment Link

| Status | คำอธิบาย |
|--------|----------|
| `ACTIVE` | ใช้งานได้ ยังไม่หมดอายุ |
| `USED` | ชำระเงินเรียบร้อยแล้ว |
| `EXPIRED` | หมดอายุ |
| `CANCELLED` | ยกเลิกโดยระบบ |

---

## 🚀 Next Steps

1. รัน SQL สร้างตาราง `payment_links`
2. ทดสอบสร้าง Payment Link
3. ทดสอบชำระเงินผ่านหน้า public
4. ตั้งค่า Webhook URL ที่ Payment Gateway
5. ทดสอบ Webhook callback

**พร้อมใช้งาน!** 🎉
